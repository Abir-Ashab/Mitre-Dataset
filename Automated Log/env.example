ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD="your password"

# Main interval (minutes)
INTERVAL_MINUTES=20

# Script names
SCREEN_RECORDING_SCRIPT=screen_recorder.py
PACKET_CAPTURE_SCRIPT=packet_capture.py
LOG_EXTRACTION_SCRIPT=extract_syslogs.py
BROWSER_LOG_SCRIPT=extract_browser_logs.py

# Output directories
BASE_OUTPUT_DIR=monitoring_outputs
SCREEN_RECORDINGS_DIR=screen_recordings
BROWSER_LOGS_DIR=logs
SYSLOGS_DIR=system_logs

# Screen recorder
SCREEN_WIDTH=1920
SCREEN_HEIGHT=1080
SCREEN_FPS=10.0
SCREEN_SEGMENT_DURATION_MINUTES=20

# Packet capture
PACKET_CAPTURE_DURATION_MINUTES=20
NETWORK_INTERFACE=5 # Run `tshark -D` to list interfaces and set the appropriate one

# Browser logs
BROWSER_API_BASE=http://localhost:5600/api/0
BROWSER_LOGS_INTERVAL_MINUTES=20

# Elasticsearch/syslogs
ELASTIC_HOST=localhost
ELASTIC_PORT=9200
ELASTIC_INDEX_PATTERN=winlogbeat-*
SYSLOGS_INTERVAL_MINUTES=20


